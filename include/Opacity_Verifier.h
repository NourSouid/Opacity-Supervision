#ifndef OPACITY_VERIFIER_H
#define OPACITY_VERIFIER_H


class Opacity_Verifier
{
    public:
        Opacity_Verifier();
        virtual ~Opacity_Verifier();

    protected:

    private:
};

#endif // OPACITY_VERIFIER_H
